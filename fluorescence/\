#script to aggreagte different spectroscopy datasets into one

import glob 


#=====import datafiles===#
datafiles = glob.glob('~/gdrive/code/edm_control/fluorescene/*.txt')

for files in datafiles:
    print(files)

